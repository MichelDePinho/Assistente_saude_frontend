Frontend melhorado com:
- UI limpa e layout responsivo simples
- Upload de logo (opcional) que é enviado ao backend como base64
- Abre PDF em nova aba para visualização antes do download

Rode:
cd frontend
npm install
npm run dev

Ajuste a URL do backend em src/App.jsx se necessário (http://localhost:3333).
